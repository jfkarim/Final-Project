<h1>All Artists</h1>

<ul class="artist-list">
<% @users.each do |user| %>
<li>
  <%= user.profile_picture.url(:icon) %>
  <%= link_to user.artist_name, user_url(user) %>
</li>
<% end %>
</ul>

<h2>The Map of Maps</h2>
<div id="map-canvas">
</div>
<%= render "map_of_maps", photos: @photos %>