<ul class="wall-show">
  <% user.wall.posts.each do |post| %>
    <%= render "post_show", user: user, post: post %>
  <% end %>
</ul>

<script>

$(document).ready(function(){

  // $(".wall-show").on("ajax:success", "#photo-delete", function(event, data){
  //   $($(event.target).parent()[0]).remove()
  // });

  $("#post-form").on("ajax:success", function(event, data){
    console.log("HERE")
    var $form = $(this);
    $(".wall-show").prepend(data);
    $form[0].reset();
  });

});

</script>